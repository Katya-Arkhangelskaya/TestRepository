package test;

public class Boolean {
    /**
    public static void main(String[] args) {
        // Пример 1
        int age = 70;
        boolean isSenior = (age > 65);
        if (isSenior)
            System.out.println("Пора на пенсию");

        // Пример 2
        int age2 = 35;
        if (age2 >= 18 && age2 <= 65)
            System.out.println("Вы можете работать");

        // Пример 3
        int age3 = 35;
        boolean isYoung3 = (age3 < 18);
        if (!isYoung3 && age3 <= 65)
            System.out.println("Вы можете работать");

        // Пример 4
        int age4 = 35;
        boolean isYoung4 = (age4 < 18);
        boolean isOld4 = (age4 > 65);
        if (!isYoung4 && !isOld4)
            System.out.println("Вы можете работать");
    }
    */





    // Оператор AND, он же И, он же &&, он же конъюнкция
    //true && true - результат true
    //true && false - результат false
    //false && true - результат false
    //false && false - результат false

    // Оператор OR, он же ИЛИ, он же ||, он же дизъюнкция
    //true && true - результат true
    //true && false - результат true
    //false && true - результат true
    //false && false - результат false

    // Оператор NOT, он же НЕ, он же !, он же инверсия
    // !true - результат false
    // !false - результат true

    // Полезные выражения
    // m && !m - результат false
    // m || !m - результат true
    // !(a && b) - результат !a || !b
    // !(a || b) - результат !a && !b
}
